---
interface Props {
  href?: string;
  class?: string;
}

const { href, class: className } = Astro.props;
const Element = href ? 'a' : 'span';
---

<Element
  href={href}
  class:list={[
    'tag',
    href && 'hover:bg-accent/20 transition-colors',
    className,
  ]}
>
  <slot />
</Element>
